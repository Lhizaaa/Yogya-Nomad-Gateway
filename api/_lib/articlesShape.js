// ---------------------------------------------------------------------------
// Kolom bersama untuk semua endpoint /api/articles/*.
// ---------------------------------------------------------------------------
export const SELECT_FIELDS = `
  id, title, title_en, category, category_en, excerpt, excerpt_en,
  image, read_time_minutes, content, content_en
`
